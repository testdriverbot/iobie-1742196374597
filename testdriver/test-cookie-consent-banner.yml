version: 4.2.19
session: 67d7d0b55f9285c9c8b81c95
steps:
  - prompt: Focus the browser window.
    commands:
      - command: focus-application
        name: Google Chrome
  - prompt: Click on "Allow all cookies."
    commands:
      - command: hover-text
        text: Allow all cookies
        description: button to accept all cookies in the cookie consent dialog
        action: click
  - prompt: >-
      Assert that the cookie consent banner disappears.  # Use the content of
      the file from the environment
    commands:
      - command: assert
        expect: the cookie consent banner is not visible
